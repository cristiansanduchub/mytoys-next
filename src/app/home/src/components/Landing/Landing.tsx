import React, { FC } from 'react';
import { Page } from '@packages/scaffolding';
export interface LandingProps {}

const Landing: FC<LandingProps> = () => {
    return (
        <Page>
            <div />
        </Page>
    );
};

export default Landing;
